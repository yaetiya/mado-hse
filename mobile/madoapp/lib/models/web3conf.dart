class EthConfig {
    static const rpc = 'https://polygon-rpc.com';
  static double fetchBalance(String address) {
    return 0;
  }
}

class PolyConfig {
  static const rpc = 'https://polygon-rpc.com';
  static double fetchBalance(String address) {
    return 0;
  }
}
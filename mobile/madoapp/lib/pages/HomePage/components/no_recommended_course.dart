import 'package:flutter/cupertino.dart';

class NoRecommendedCourse extends StatelessWidget {
  const NoRecommendedCourse({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
